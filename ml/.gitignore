# 原始抽样、外部来源数据、训练产物不入库；标注数据（data/labeled/）是长期资产，必须提交。
# 例外：external_pool.jsonl.gz 是 517MB 外部库删除后的唯一替代资产（几 MB），入库保存。
data/raw/
data/sources/*
!data/sources/external_pool.jsonl.gz
artifacts/
.venv/
*.lock
